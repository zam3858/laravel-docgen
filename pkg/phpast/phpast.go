package phpast

import "fmt"

// ParseFile is a stub for the PHP AST integration layer.
func ParseFile(path string) ([]byte, error) {
	return nil, fmt.Errorf("php ast parser not yet configured for %s", path)
}
